<Window x:Class="LogAnalyzer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogAnalyzer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="1" Height="20" Width="230" Margin="5" x:Name="UserSearchBox" TextChanged="UserSearchBox_TextChanged"/>
        <ListView Grid.Row="1" Grid.ColumnSpan="2"
                  x:Name="LineView"
                  ItemsSource="{Binding RenderedLines}"
                  VirtualizingPanel.IsVirtualizing="False"
                  VirtualizingPanel.VirtualizationMode="Standard"
                  ScrollViewer.CanContentScroll="True"
        />

            <Menu>
            <MenuItem Header="File">
                <MenuItem Header="Open File" Click="OpenMenuItem_Click"/>
                <MenuItem Header="Close File" Click="CloseMenuItem_Click"/>
            </MenuItem>
        </Menu>
    </Grid>
</Window>
